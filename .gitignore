# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# test coverage
/coverage/

# env
.env
.env*.local

# data — real booking submissions, keep out of git
/data/bookings.json
/data/bookings.json.imported
/data/*.db
/data/*.db-wal
/data/*.db-shm

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Deploy config (holds env-specific values; use fly.toml.example as the template).
# Not committed — configuration lives in `fly secrets`.
fly.toml
